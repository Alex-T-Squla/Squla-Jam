using SimpleJson;
using Squla.Core.IOC;
using Squla.App.{Namespace};

namespace Squla.TDD
{
	public class ux_{Component} : ComponentTesterV2
	{
		[InjectComponent ("{Component}")]
		private {Component} {ComponentLowerCase};

        private void ApplyModel(JsonObject data)
        {
            var model = modelizer.Modelize<{ComponentModel}> (data);

            {ComponentLowerCase}.ApplyModel(model);
        }
    }
}